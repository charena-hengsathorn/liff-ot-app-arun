# ===========================================
# VERCEL MIGRATION - ENVIRONMENT VARIABLES
# ===========================================
# This file shows which environment variables are needed for Vercel deployment.
# DO NOT commit .env.local or .env files to Git!

# ===========================================
# FRONTEND ENVIRONMENT VARIABLES (VITE_*)
# These must be set in Vercel Dashboard
# ===========================================

# LINE LIFF Configuration
VITE_LIFF_ID=your-liff-id-here

# Google Sheets IDs
VITE_GOOGLE_SHEET_ID_DEV=your-dev-sheet-id
VITE_GOOGLE_SHEET_ID_PROD=your-prod-sheet-id

# API Backend URLs
# Development: Local backend
VITE_API_BASE_URL_DEV=http://localhost:3001

# Production: Heroku backend
VITE_API_BASE_URL_PROD=https://your-heroku-app.herokuapp.com

# ===========================================
# BACKEND ENVIRONMENT VARIABLES
# These must be set in Heroku Dashboard
# ===========================================

# Node Environment
NODE_ENV=production

# Database
DATABASE_URL=postgresql://user:pass@host:5432/dbname

# Strapi Configuration
STRAPI_URL=http://localhost:1337

# Google Service Account (Base64 encoded JSON)
GOOGLE_SERVICE_ACCOUNT_KEY=base64-encoded-service-account-key
# OR use file path (for local development)
GOOGLE_SERVICE_ACCOUNT_KEY_FILE=./google-credentials.json

# LINE Messaging API
LINE_CHANNEL_ACCESS_TOKEN=your-line-channel-access-token
LINE_GROUP_ID_DEV=your-dev-group-id
LINE_GROUP_ID_PROD=your-prod-group-id

# Manager User IDs (comma-separated)
MANAGER_USER_IDS_DEV=user-id-1,user-id-2
MANAGER_USER_IDS_PROD=user-id-1,user-id-2

# DevAdmin Authentication
DEVADMIN_USERNAME=devadmin
DEVADMIN_PASSWORD_HASH=bcrypt-hashed-password
JWT_SECRET=your-jwt-secret-key
JWT_EXPIRY=24h

# Admin JWT Secret
ADMIN_JWT_SECRET=your-admin-jwt-secret

# Google Apps Script ID (optional)
GOOGLE_APPS_SCRIPT_ID=your-script-id
